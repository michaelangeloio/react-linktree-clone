import {makeStyles} from '@mui/styles';

const useStyles = makeStyles((theme) => ({
    menuIconMain : {
        
        color: 'white'
    },
  menuMainText: {
    color: "white",
  }
}));

export default useStyles;
